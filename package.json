{
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "serverless deploy"
  },
  "dependencies": {
    "mongoose": "^8.0.2",
    "serverless": "^3.38.0",
    "stripe": "^14.5.0"
  }
}
